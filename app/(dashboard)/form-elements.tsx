import { Text } from 'tamagui'

import { PageShell } from '@/components/PageShell'

export default function Page() {
  return (
    <PageShell title="Form Elements">
      <Text color="$gray10" fontSize={14}>
        Form Elements — content ported in a later phase. Navigation and theme shell are live.
      </Text>
    </PageShell>
  )
}
